# terraform.tfvars
#
# このファイルは、variables.tfで定義された変数に具体的な値を設定します。
# プロジェクト固有のパラメータをここで指定します。

aws_region          = "ap-northeast-1"
lambda_function_name = "api-lambda-function"
stage               = "dev"
